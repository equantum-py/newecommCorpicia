import { notFound } from 'next/navigation';import { getAdminProducts } from '@/lib/repositories/admin';import { getAdminCollection } from '@/lib/repositories/collections';import CollectionEditor from '@/components/admin/CollectionEditor';export const dynamic='force-dynamic';export default async function Page({params}:{params:Promise<{id:string}>}){const {id}=await params;const [collection,products]=await Promise.all([getAdminCollection(id),getAdminProducts()]);if(!collection)notFound();return <CollectionEditor collection={collection} products={products}/>}